package com.sdacademy.recursivity.tema2;

import java.util.ArrayList;

//Adaugati intru-un array cu 10 elemente de tip int(numere random).Folositi recursivitate
public class PopulareArray {

    public void populareArray(ArrayList<Integer> arrayList){

        if(arrayList.size() >= 10){
            return;
        }
        s/
        int numr =
         arrayList.add(numr);
        populareArray(arrayList);

    }

    public void adaugare (){

        ArrayList<Integer> arlist = new ArrayList<Integer>();
         populareArray(arList);
        System.out.println(arlist);
    }
}
